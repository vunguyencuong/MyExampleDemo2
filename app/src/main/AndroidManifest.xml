<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyExampleDemo2"
        android:name=".ThingSmartApp"
        tools:replace="android:allowBackup, android:supportsRtl"
        tools:targetApi="31">

        <meta-data
            android:name="THING_SMART_APPKEY"
            android:value="8543qrxpgu93d9rjhwkk" />
        <meta-data
            android:name="THING_SMART_SECRET"
            android:value="udm8aagt39rw3v3rf8j7krcpe9s4h9tc" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>